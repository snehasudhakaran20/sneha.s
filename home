<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sneha Sudhakaran</title>
    <style>
        body {
            margin: 0;
            font-family: 'Georgia', serif;
            background: linear-gradient(135deg, #d4dbc3, #fde2e2); /* Olive and peach/pink pastel gradient */
            color: #333;
            line-height: 1.6;
            position: relative;
        }

        header {
            background: #f4f4f4;
            padding: 20px;
            border-bottom: 2px solid #ddd;
            display: flex;
            align-items: center;
        }

        header img {
            width: 127.5px; /* Reduced size by 15% */
            height: 127.5px;
            margin-right: 20px;
            border-radius: 50%;
        }

        header h1 {
            margin: 0;
            font-size: 2.5rem;
            text-align: left;
        }

        #dynamic-container {
            margin-left: 0; /* Align with the name header */
        }

        #dynamic-container #dynamic-sentence {
            font-family: 'Courier New', monospace;
            font-size: 1.2rem;
            color: #555;
        }

        nav {
            text-align: center;
            margin-top: 10px;
        }

        nav a {
            color: #007acc;
            text-decoration: none;
            margin: 0 15px;
            font-size: 1.2rem;
        }

        nav a:hover {
            text-decoration: underline;
        }

        main {
            padding: 20px;
            max-width: 800px;
            margin: 0 auto;
        }

        .links {
            display: flex;
            justify-content: space-around;
            margin: 20px 0;
        }

        .link-item {
            text-align: center;
        }

        .link-item img {
            width: 50px;
            height: 50px;
        }

        .link-item a {
            display: block;
            margin-top: 5px;
            font-size: 1rem;
            color: #007acc;
            text-decoration: none;
        }

        .link-item a:hover {
            text-decoration: underline;
        }

        footer {
            text-align: center;
            padding: 10px 0;
            background: #f4f4f4;
            border-top: 2px solid #ddd;
            font-size: 0.9rem;
        }
    </style>
</head>
<body>
    <header>
        <img src="profilepic.jpg" alt="Profile Image">
        <div>
            <h1>Sneha S</h1>
            <div id="dynamic-container">
                <div id="dynamic-sentence"></div>
            </div>
        </div>
    </header>

    <nav>
        <a href="Index.html">Home</a>
        <a href="Resume.html">Resume</a>
        <a href="Contact.html">Contact</a>
    </nav>

    <main>
        <h2>Thank you for visiting my site</h2>
        <p>I'm a computer engineer interested in digital design and physics. Have a bunch of project ideas I plan to work on for my research in graphics and camera systems. I would love to collaborate, please feel free to reach out.</p>

        <div class="links">
            <div class="link-item">
                <img src="physics.jpg" alt="Physics">
                <a href="physics.html">Physics</a>
            </div>
            <div class="link-item">
                <img src="computers.jpeg" alt="Computers">
                <a href="computers.html">Computers</a>
            </div>
            <div class="link-item">
                <img src="ethics.jpeg" alt="Ethics">
                <a href="ethics.html">Ethics</a>
            </div>
        </div>
    </main>

    <footer>
        &copy; 2024 Blog. All rights reserved.
    </footer>

    <script>
        const sentences = ["Digital Design Engineer", "Physics Computers Ethics"];
        const dynamicSentence = document.getElementById('dynamic-sentence');
        let currentSentence = Array(sentences[0].length).fill('_').join('');
        let currentTargetIndex = 0;

        function scrambleAndReveal() {
            let targetSentence = sentences[currentTargetIndex];
            let updatedSentence = '';
            for (let i = 0; i < targetSentence.length; i++) {
                if (currentSentence[i] === targetSentence[i]) {
                    updatedSentence += targetSentence[i];
                } else {
                    updatedSentence += Math.random() < 0.1 ? targetSentence[i] : getRandomChar();
                }
            }
            currentSentence = updatedSentence;
            dynamicSentence.innerHTML = currentSentence; // Remove colorization of characters

            if (currentSentence !== targetSentence) {
                setTimeout(scrambleAndReveal, 50);
            } else {
                setTimeout(scrambleToNextSentence, 4000); // Increased time for sentence visibility
            }
        }

        function scrambleToNextSentence() {
            currentTargetIndex = (currentTargetIndex + 1) % sentences.length;
            currentSentence = Array(sentences[currentTargetIndex].length).fill('_').join('');
            scrambleAndReveal();
        }

        function getRandomChar() {
            const chars = 'AUxyzutvsSOQEHKLklqRrTZYX0123456789!@#$%^&*()<>_=';
            return chars[Math.floor(Math.random() * chars.length)];
        }

        scrambleAndReveal();
    </script>
</body>
</html>
